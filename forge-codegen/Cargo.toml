[package]
name = "forge-codegen"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Code generation for Forge: Rust AST to CUDA PTX/SPIR-V"

[dependencies]
forge-core = { path = "../forge-core" }
